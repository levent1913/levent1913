
aws events put-rule \
--name omo1 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo2 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo3 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo4 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo5 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo6 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo7 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo8 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo9 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo10 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo11 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo12 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo13 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo14 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo15 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo16 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo17 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo18 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo19 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo20 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo21 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1

aws events put-rule \
--name omo22 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo23 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


aws events put-rule \
--name omo24 \
--schedule-expression 'cron(0-59 * * * ? *)' \
--region us-east-1


